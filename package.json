{
  "name": "reflect",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered training simulator for psychology students — practice therapy sessions with simulated clients and get supervisor-style feedback.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "concurrently --names api,web --prefix-colors blue,magenta -k npm:start:api npm:start:web",
    "start:api": "npm --workspace backend run start:dev",
    "start:web": "npm --workspace frontend start",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "db:push": "npm --workspace backend run db:push"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "overrides": {
    "rxjs": "7.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}
